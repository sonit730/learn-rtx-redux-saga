import * as React from 'react';

export interface AdminLayoutProps {
}

export function AdminLayout (props: AdminLayoutProps) {
  // console.log("🚀 ~ file: Admin.tsx ~ line 7 ~ AdminLayout ~ props", props)
  return (
    <div>
      Admin Layout
    </div>
  );
}
